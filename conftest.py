"""Empty on purpose: its presence makes pytest add the repo root to
sys.path, so `import dpgcore` / `import convqueue` work from test/ without
installing the packages or hand-editing sys.path in every test file."""
